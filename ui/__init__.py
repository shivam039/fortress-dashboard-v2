"""
Fortress UI layer — top-level package.

Subpackages:
  ui.state        Central session-state manager & app-wide constants
  ui.components   Reusable Streamlit widgets (auth, broker, orders, …)
  ui.utils        Pure helpers — formatting, scan runners, telegram
  ui.views        One render() entry-point per dashboard module
"""
