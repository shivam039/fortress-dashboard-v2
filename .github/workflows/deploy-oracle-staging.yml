name: Deploy Oracle Staging

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: oracle-staging-deploy
  cancel-in-progress: false

env:
  IMAGE_NAME: ghcr.io/${{ github.repository }}/fortress-backend

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create source archive
        run: git archive --format=tar.gz --output=fortress-source.tgz HEAD

      - name: Prepare SSH
        env:
          ORACLE_SSH_PRIVATE_KEY: ${{ secrets.ORACLE_SSH_PRIVATE_KEY }}
          ORACLE_KNOWN_HOSTS: ${{ secrets.ORACLE_KNOWN_HOSTS }}
        run: |
          set -euo pipefail
          test -n "$ORACLE_SSH_PRIVATE_KEY"
          test -n "$ORACLE_KNOWN_HOSTS"
          install -m 700 -d ~/.ssh
          printf '%s\n' "$ORACLE_SSH_PRIVATE_KEY" > ~/.ssh/oracle_staging
          chmod 600 ~/.ssh/oracle_staging
          printf '%s\n' "$ORACLE_KNOWN_HOSTS" > ~/.ssh/known_hosts

      - name: Upload source and deployment files
        env:
          ORACLE_HOST: ${{ secrets.ORACLE_HOST }}
          ORACLE_USER: ${{ secrets.ORACLE_USER }}
        run: |
          set -euo pipefail
          test -n "$ORACLE_HOST"
          test -n "$ORACLE_USER"
          ssh -i ~/.ssh/oracle_staging "$ORACLE_USER@$ORACLE_HOST" "sudo mkdir -p /opt/fortress-dashboard && sudo chown $ORACLE_USER:$ORACLE_USER /opt/fortress-dashboard"
          ssh -i ~/.ssh/oracle_staging "$ORACLE_USER@$ORACLE_HOST" "mkdir -p /opt/fortress-dashboard/source/${{ github.sha }}"
          scp -i ~/.ssh/oracle_staging fortress-source.tgz "$ORACLE_USER@$ORACLE_HOST:/opt/fortress-dashboard/source/${{ github.sha }}/"
          ssh -i ~/.ssh/oracle_staging "$ORACLE_USER@$ORACLE_HOST" "cd /opt/fortress-dashboard/source/${{ github.sha }} && tar -xzf fortress-source.tgz && rm fortress-source.tgz"
          scp -i ~/.ssh/oracle_staging docker-compose.oracle-staging.yml Caddyfile "$ORACLE_USER@$ORACLE_HOST:/opt/fortress-dashboard/"
          scp -i ~/.ssh/oracle_staging scripts/deploy-oracle-staging.sh "$ORACLE_USER@$ORACLE_HOST:/opt/fortress-dashboard/deploy-oracle-staging.sh"
          ssh -i ~/.ssh/oracle_staging "$ORACLE_USER@$ORACLE_HOST" "chmod +x /opt/fortress-dashboard/deploy-oracle-staging.sh"

      - name: Build ARM64 image on Oracle VM
        env:
          ORACLE_HOST: ${{ secrets.ORACLE_HOST }}
          ORACLE_USER: ${{ secrets.ORACLE_USER }}
          FORTRESS_IMAGE: ${{ env.IMAGE_NAME }}:${{ github.sha }}
        run: |
          set -euo pipefail
          ssh -i ~/.ssh/oracle_staging "$ORACLE_USER@$ORACLE_HOST" \
            "cd /opt/fortress-dashboard/source/${{ github.sha }} && docker build -t '$FORTRESS_IMAGE' ."

      - name: Deploy and verify health
        env:
          ORACLE_HOST: ${{ secrets.ORACLE_HOST }}
          ORACLE_USER: ${{ secrets.ORACLE_USER }}
          ORACLE_STAGING_BACKEND_URL: ${{ secrets.ORACLE_STAGING_BACKEND_URL }}
          FORTRESS_IMAGE: ${{ env.IMAGE_NAME }}:${{ github.sha }}
        run: |
          set -euo pipefail
          test -n "$ORACLE_STAGING_BACKEND_URL"
          ssh -i ~/.ssh/oracle_staging "$ORACLE_USER@$ORACLE_HOST" \
            "FORTRESS_IMAGE='$FORTRESS_IMAGE' FORTRESS_SKIP_IMAGE_PULL=1 ORACLE_STAGING_BACKEND_URL='$ORACLE_STAGING_BACKEND_URL/api/health' /opt/fortress-dashboard/deploy-oracle-staging.sh"
